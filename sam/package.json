{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "deploy": "npm run build && sam deploy --no-confirm-changeset --parameter-overrides GraphQLURI=${GRAPHQL_URI}"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
